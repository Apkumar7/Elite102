# Database configuration stored separately for modularity.
# I initially hardcoded the config in main.py, which made testing harder.

db_config = {
    'host': 'localhost',
    'user': 'root',
    'password': 'K092509s',
    'database': 'bank_user_info'
}
